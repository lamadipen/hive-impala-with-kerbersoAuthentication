# hive-impala-with-kerbersoAuthentication
In the example I am using kerberos authentication to authenticate and connect to impala and hive. After getting the data it is populated into the oracle database.
